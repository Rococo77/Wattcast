# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Pipeline artefacts — regenerated by the ML steps, never committed.
# Anchored to the repo root so they do NOT match web/public/data, which holds
# the committed dashboard snapshot.
/data/
/model/

# OS / editor
.DS_Store
.idea/
.vscode/
